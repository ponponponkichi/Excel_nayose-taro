# Excel_nayose-taro
顧客リスト等で「株式会社A」と「A（株）」、「高橋太郎」と「髙橋 太郎」のように、完全に一致していないが「実質同じ」データ（表記揺れによる重複）を、何千件もある膨大なリストの中から目視で探す手間をなくしたい。指定した列のデータを自動でクレンジング（法人格などのジャマな文字を一時除外）した上で類似度を計算し、「設定した〇%以上似ているデータ」をグループ化してExcelに出力します。確認者（人間）は出力されたExcelで「〇・×」をつけるだけで、代表データに統一された最終リストがワンクリックで完成します。
